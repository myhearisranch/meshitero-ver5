<div>
  <h3><%= @user.name %></h3>
  <%= image_tag @user.get_profile_image("100x100") %>

  <% if @user == current_user %>
     <p><%= link_to "プロフィール編集", edit_user_path %></p>
  <% end %>
</div>

<%# 違うフォルダに部分テンプレートがあるため、フォルダ名も指定する必要がある %>
<%= render 'postimages/list' , post_images: @post_images %>